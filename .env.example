# Origin of the flashcards sync API (e.g. a Cloudflare Worker), without trailing slash.
# Required at build time — the app reads it via $env/static/public.
# PUBLIC_API_BASE_URL=https://your-worker.example.com
PUBLIC_API_BASE_URL=